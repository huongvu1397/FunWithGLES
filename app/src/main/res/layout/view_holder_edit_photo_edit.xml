<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/edit_photo_edit_view_holder_width"
    android:layout_height="@dimen/edit_photo_edit_list_height">

    <ImageView
        android:id="@+id/img_thumbnail"
        android:layout_width="@dimen/edit_photo_edit_view_holder_width"
        android:layout_height="@dimen/edit_photo_edit_view_holder_width"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/colorPrimary" />

    <TextView
        android:id="@+id/txt_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="@color/black"
        android:textSize="10dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/img_thumbnail" />

    <View
        android:id="@+id/item_modified"
        android:layout_width="@dimen/beauty_list_item_dot_size"
        android:layout_height="@dimen/beauty_list_item_dot_size"
        android:background="@drawable/beauty_detail_item_modified_mark"
        android:backgroundTint="#ffffff"
        android:visibility="gone"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@+id/img_thumbnail" />
</androidx.constraintlayout.widget.ConstraintLayout>